# Chaologies 网站工作规范

## 中文文案与编辑式断行

中文页面的重点文案不能依赖浏览器自动换行。先确定句子和语义节奏，再决定视觉断行。

1. 先把中文说顺。大声读一遍，主语、语序和停顿自然后再排版；不要靠断行掩盖语病。
2. 一条编辑行只表达一个完整的意思。优先在句号、问号、冒号或自然分句处断开，不拆开主语与谓语、修饰语与中心词。
3. 同一文案块通常控制在 2–4 条编辑行。相邻两行的视觉长度应大致接近，除非短句被明确用作开场或结论；避免无意形成“短—长—短”的轮廓。
4. 重要中文标题、引导语和观点文案使用 `src/EditorialLines.jsx` 明确写出语义行。不要用空格凑换行，也不要只靠容器宽度碰运气。
5. 桌面和平板保留编辑断行。手机端允许单条编辑行在内部继续自然折行，不使用 `white-space: nowrap`，不为了守住一行而缩小到难以阅读。
6. 标题可使用 `text-wrap: balance`，正文使用 `text-wrap: pretty`；它们只能优化行尾，不能代替人工确定语义行。
7. 交付前至少检查约 390、768 和 1280 像素三种宽度，并检查是否出现孤零零的代词、连词、标点或不足半行的意外残句。

示例：

```jsx
<EditorialLines
  lines={[
    "当时间不再是一个模糊的“以后”，",
    "我们自然会更清楚，哪些事值得我们的时间。",
  ]}
/>
```
